{
  "name": "@waa/cli",
  "version": "2.0.0",
  "description": "Web Access Advisor command-line interface over @waa/core: single-page analyze, recording replay, session listing",
  "private": true,
  "type": "module",
  "bin": {
    "waa": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@waa/core": "^2.0.0",
    "@waa/shared": "^2.0.0",
    "zod": "^4.0.0"
  }
}
