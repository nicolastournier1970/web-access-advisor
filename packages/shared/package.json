{
  "name": "@waa/shared",
  "version": "3.0.0",
  "description": "Web Access Advisor shared contracts: zod schemas for the recording format, session manifest, API DTOs and SSE events",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
